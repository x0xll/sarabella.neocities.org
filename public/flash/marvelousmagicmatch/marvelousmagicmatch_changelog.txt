05/08/2025:
    - Trying to override the WS calls with the JS functions
        - Remove the "this.webService" calls in MagicMatchService constructor
        - Replace the "this.webService" call in InitMagicMatch() in MagicMatchService.as with:
            var result:Object = ExternalInterface.call("initMagicMatch");
            initMMResultHandler(result);
        - Add "import flash.external.*;" in MagicMatchService.as
        - Remove "super()" and error checks in MagicMatchService constructor
        - Add following lines to ShellConnector constructor, and remove them from SetServiceProvider() in ShellConnector.as
            m_magicMatchService = new MagicMatchService(Globals.getInst().magicMatchInst,m_lang);
            m_magicMatchService.InitMagicMatch();
        - Remove param1 and 2 of MagicMatchService constructor, rename param3 and 4 to be param1 and 2. Clean up the function accordingly
    - Fix paths
        - Change the .swf path in smokeSwitchCB_Prize() in InitState.as to point to "./swf/" instead
        - Change to "_digiObjPath = "./swf/" + param3 + ".swf";" in setObjectData() in Box.as